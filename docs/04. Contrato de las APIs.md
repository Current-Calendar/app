# Contrato de las APIs

## Propósito

El presente documento tiene la intención de informar a los desarrolladores de Current sobre la arquitectura de API REST del proyecto. A continuación se encuentran las rutas a emplear separadas por módulo.

> **Nota:** Todas las rutas comienzan desde la raíz del servidor (p. ej. `http://localhost:8000`). Los errores estándar devuelven `{ "errors": ["..."] }` o `{ "error": "..." }` con el código HTTP correspondiente.

---

## Gestión de users

| Método | Ruta | Descripción | Respuesta (éxito) |
| :--- | :--- | :--- | :--- |
| `POST` | `/api/v1/auth/registro/` | Registro de nuevo user | `201` `{ "message": "User registrado exitosamente", "user": { id, username, email, ... } }` |
| `POST` | `/api/v1/auth/login/` | Inicio de sesión | _Pendiente de implementar_ |
| `POST` | `/api/v1/auth/logout/` | Cierre de sesión | _Pendiente de implementar_ |
| `POST` | `/api/v1/auth/password_reset/` | Solicitar email de restablecimiento | _Pendiente de implementar_ |
| `POST` | `/api/v1/auth/password_reset/confirm/` | Cambiar contraseña con el token del email | _Pendiente de implementar_ |
| `PUT` | `/api/v1/users/me` | Editar perfil propio | `200` Datos del user actualizados (UserSerializer) |
| `DELETE` | `/api/v1/users/me` | Eliminar cuenta propia | `202` `{ "message": "User eliminado satisfactoriamente" }` |
| `GET` | `/api/v1/users?search=<query>` | Buscar users por username, email o pronouns | `200` `[ { id, username, email, pronouns, bio, photo, total_seguidores, total_following, total_calendarios_following }, ... ]` |
| `POST` | `/api/v1/users/{id}/follow/` | Seguir / dejar de seguir a un user | `200` `{ "user": <id>, "followed": <bool> }` |
| `POST` | `/api/v1/users/{id}/block/` | Bloquear a un user | _Pendiente de implementar_ |

**Body de `/api/v1/auth/registro/`:**
```json
{
  "username": "string",
  "email": "string",
  "password": "string",
  "password2": "string"
}
```

---

## Gestión de calendars

| Método | Ruta | Descripción | Respuesta (éxito) |
| :--- | :--- | :--- | :--- |
| `POST` | `/api/v1/calendars` | Crear un nuevo calendar | `201` `{ id, origin, external_id, name, description, privacy, creator_id, created_at }` |
| `GET` | `/api/v1/calendars/list` | Listar calendars (con filtros opcionales `?q=<name>&privacy=<PRIVATE\|FRIENDS\|PUBLIC>`) | `200` `[ { id, name, description, privacy, origin, creator_id, creator_username, created_at }, ... ]` |
| `PUT` | `/api/v1/calendars/{id}/publicar` | Publicar un calendar (cambia privacy a PUBLIC) | `200` `{ id, name, description, privacy, origin, creator, created_at }` |
| `PUT` / `PATCH` | `/api/v1/calendars/{id}/editar/` | Editar name, descripción o privacy de un calendar | `200` `{ id, name, description, privacy }` |
| `DELETE` | `/api/v1/calendars/{id}/eliminar/` | Eliminar un calendar (solo el creator) | `204` Sin contenido |

**Body de `POST /api/v1/calendars`:**
```json
{
  "creator_id": "<int>",
  "name": "string",
  "description": "string (opcional)",
  "privacy": "PRIVATE | FRIENDS | PUBLIC (opcional, default: PRIVATE)",
  "origin": "CURRENT | GOOGLE | APPLE (opcional, default: CURRENT)",
  "external_id": "string (opcional)"
}
```

**Body de `PUT/PATCH /api/v1/calendars/{id}/editar/`:**
```json
{
  "name": "string (opcional)",
  "description": "string (opcional)",
  "privacy": "PRIVATE | FRIENDS | PUBLIC (opcional)"
}
```

---

## Gestión de events

| Método | Ruta | Descripción | Respuesta (éxito) |
| :--- | :--- | :--- | :--- |
| `POST` | `/api/v1/events` | Crear un nuevo event | `201` `{ id, title, description, place_name, date, time, recurrence, external_id, calendars: [int], created_at }` |
| `PUT` | `/api/v1/events/{id}` | Editar un event existente | `200` `{ id, title, description, place_name, date, time, recurrence, external_id, calendars: [int], created_at }` |
| `GET` / `DELETE` | `/api/v1/events/{id}` | Detalle / borrado de event | _Pendiente de implementar_ |
| `POST` | `/api/events/asignar/` | Asignar un event a un calendar | `200` `{ "mensaje": "Event '<title>' asignado al calendar '<name>'" }` |
| `DELETE` | `/api/events/desasignar/` | Desasignar un event de un calendar | `200` `{ "mensaje": "Event '<title>' desasignado del calendar '<name>'" }` |

**Body de `POST /api/v1/events`:**
```json
{
  "title": "string",
  "date": "YYYY-MM-DD",
  "time": "HH:MM:SS",
  "calendars": [<calendario_id>, ...],
  "creator_id": "<int>",
  "description": "string (opcional)",
  "place_name": "string (opcional)",
  "latitud": "<float> (opcional)",
  "longitud": "<float> (opcional)",
  "recurrence": "string (opcional)",
  "external_id": "string (opcional)"
}
```

**Body de `POST /api/events/asignar/` y `DELETE /api/events/desasignar/`:**
```json
{
  "evento_id": "<int>",
  "calendario_id": "<int>"
}
```

---

## Importación / Exportación de calendars

| Método | Ruta | Descripción | Respuesta (éxito) |
| :--- | :--- | :--- | :--- |
| `GET` | `/api/v1/google-auth` | Iniciar flujo OAuth2 de Google Calendar | `302` Redirección a Google OAuth |
| `GET` / `POST` | `/api/calendars/import-google-calendar` | Importar events desde Google Calendar (requiere sesión OAuth2 activa) | `200` `{ "message": "Eventos importados exitosamente", "count": <int> }` |
| `POST` | `/api/calendars/import-ios-calendar` | Importar calendar desde iOS Calendar vía URL webcal | `200` `{ "message": "Calendar iOS importado exitosamente", "count": <int> }` |
| `POST` | `/api/calendars/import-ics` | Importar calendar desde archivo `.ics` subido | `200` `{ "message": "Archivo ICS importado exitosamente" }` |
| `GET` | `/api/calendars/{id}/export` | Exportar un calendar a formato `.ics` | `200` Archivo adjunto `text/calendar` |

**Body de `POST /api/calendars/import-ios-calendar`:**
```json
{
  "webcal_url": "webcal://... o https://...",
  "user": "<int>",
  "privacy": "PRIVATE | FRIENDS | PUBLIC (opcional, default: PRIVATE)"
}
```

**Body de `POST /api/calendars/import-ics` (multipart/form-data):**
```
file: <archivo .ics>
user: <int>
privacy: PRIVATE | FRIENDS | PUBLIC (opcional, default: PRIVATE)
```

---

## Gestión de calendars públicos

| Método | Ruta | Descripción | Respuesta (éxito) |
| :--- | :--- | :--- | :--- |
| `GET` | `/api/v1/comments/?target_type=<EVENT\|CALENDAR>&target_id=<id>&cursor=<cursor>&limit=<n>&sort=<new\|old>` | Listar comentarios raí­z de un event/calendar | `200` `{ results: [...], next_cursor, has_more }` |
| `POST` | `/api/v1/comments/` | Crear comentario raí­z o respuesta (`parent_id` opcional) | `201` Comentario creado |
| `GET` | `/api/v1/comments/{comment_id}/replies/?cursor=<cursor>&limit=<n>&sort=<new\|old>` | Listar respuestas de un comentario raí­z | `200` `{ results: [...], next_cursor, has_more }` |
| `DELETE` | `/api/v1/comments/{comment_id}/delete/` | Borrado permanente (autor o creador del target) | `204` Sin contenido |
| `POST` | `/api/v1/events/{id}/save/` | Guardar event público a mi calendar personal | _Pendiente de implementar_ |
| `GET` | `/api/v1/public/explore/` | Feed de events recomendados | _Pendiente de implementar_ |
| `GET` | `/api/v1/public/search/` | Búsqueda por palabra clave, etiqueta o filtro | _Pendiente de implementar_ |
| `POST` | `/api/v1/calendars/{id}/report/` | Reportar calendar | _Pendiente de implementar_ |
| `GET` | `/api/v1/calendars/{id}/stats/` | Estadísticas del calendar | _Pendiente de implementar_ |
| `POST` | `/api/v1/calendars/{id}/mute/` | Silenciar calendar | _Pendiente de implementar_ |
| `POST` | `/api/v1/events/{id}/report/` | Reportar event | _Pendiente de implementar_ |

---

## Gestión de chats

| Método | Ruta | Descripción | Respuesta (éxito) |
| :--- | :--- | :--- | :--- |
| `GET` | `/api/v1/chat/conversations/` | Listado de chats | _Pendiente de implementar_ |
| `GET` / `POST` | `/api/v1/chat/{user_id}/` | Ver historial de mensajes o enviar un mensaje | _Pendiente de implementar_ |

---

## Gestión de notificaciones

| Método | Ruta | Descripción | Respuesta (éxito) |
| :--- | :--- | :--- | :--- |
| `GET` | `/api/v1/notifications/` | Centro de notificaciones | _Pendiente de implementar_ |
| `PUT` | `/api/v1/users/me/settings/` | Configurar notificaciones | _Pendiente de implementar_ |

---

## Administración

| Método | Ruta | Descripción | Respuesta (éxito) |
| :--- | :--- | :--- | :--- |
| `GET` / `PUT` / `DELETE` | `/api/v1/admin/users/{id}/` | CRUD de users para administrador | _Pendiente de implementar_ |
| `GET` | `/api/v1/admin/reports/` | Ver y gestionar reportes de contenido ofensivo | _Pendiente de implementar_ |
| `POST` | `/api/v1/admin/users/{id}/suspend/` | Suspender cuenta de user | _Pendiente de implementar_ |
| `GET` | `/api/v1/admin/metrics/` | Ver métricas del sistema | _Pendiente de implementar_ |
